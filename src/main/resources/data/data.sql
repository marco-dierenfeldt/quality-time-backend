-- DROP SEQUENCE IF EXISTS SEQ_TIME_ENTRY_ID;
DROP TABLE IF EXISTS TIME_ENTRY;

-- CREATE SEQUENCE SEQ_TIME_ENTRY_ID START WITH 1 INCREMENT BY 1;

CREATE TABLE TIME_ENTRY(
  --ID BIGINT DEFAULT SEQ_TIME_ENTRY_ID.NEXTVAL PRIMARY KEY,
  ID INT PRIMARY KEY,
  BEGIN_DATE  TIMESTAMP,
  END_DATE    TIMESTAMP,
  DESCRIPTION VARCHAR(255)
);

COMMIT;

INSERT INTO TIME_ENTRY ( ID, BEGIN_DATE , END_DATE , DESCRIPTION) VALUES (1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP+1, 'Erster Task');
INSERT INTO TIME_ENTRY ( ID, BEGIN_DATE , END_DATE , DESCRIPTION) VALUES (2, CURRENT_TIMESTAMP+1, CURRENT_TIMESTAMP+2, 'Zweiter Task');

COMMIT;